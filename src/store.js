const initailState = {
  blance: 0,
  loan: 0,
  loanPurpose: "",
};

function reducer(state = initailState, action) {
  switch (action.type) {
    case value:
      break;

    default:
      break;
  }
}
